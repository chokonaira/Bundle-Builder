server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Hashed assets can cache forever; the shell shouldn't.
  location /assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable";
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
